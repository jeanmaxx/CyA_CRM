document.getElementById('lead-form')?.addEventListener('submit',function(e){e.preventDefault();const data=new FormData(this);const name=data.get('name')||'Gracias';const status=document.getElementById('form-status');status.textContent=name+', tu solicitud quedó preparada para la siguiente fase de integración comercial.';status.style.color='#ffc20e';this.querySelector('button').textContent='Solicitud preparada ✓';});